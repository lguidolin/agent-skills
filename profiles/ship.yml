name: ship
description: "Push, PR, archive decisions, merge cleanup"

# finishing-a-development-branch and requesting/receiving-code-review come
# from the superpowers plugin — see `plugins:` below.
skills:
  - ship-it
  - conventional-commits-and-releases
  - resilience-and-deploy-safety

plugins:
  - superpowers

mcps:
  - context7

claudeignore:
  - "docs/superpowers/archive/**"

hooks:
  pre_session: true
  post_session: true
